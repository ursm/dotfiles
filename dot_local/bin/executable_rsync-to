#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 2 ]]; then
  echo "Usage: rsync-to HOST PATH [PATH...]" >&2
  exit 1
fi

host=$1; shift

for path in "$@"; do
  abs=$(realpath "$path")
  rsync -avz "$abs" "${host}:$(dirname "$abs")/"
done
