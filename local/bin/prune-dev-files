#!/bin/sh

for f in $(fd config.ru $(git config --get ghq.root)); do
  root=$(dirname $(realpath $f))

  fd $root 'log'             --extension log --changed-before 1d --no-ignore --exec-batch rm
  fd $root 'tmp/screenshots' --extension png --changed-before 1d --no-ignore --exec-batch rm
done
